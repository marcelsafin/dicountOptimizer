"""Integration tests for agent composition and full pipeline testing.

This package contains integration tests that validate the complete agent
pipeline with mocked repositories, error propagation, retry logic, and
caching behavior.
"""
