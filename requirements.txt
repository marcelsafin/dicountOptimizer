# Core dependencies
google-adk
python-dotenv
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Web framework (async-capable)
flask
asgiref>=3.7.0

# Production ASGI server
gunicorn>=21.0.0
uvicorn[standard]>=0.24.0

# HTTP client (async with connection pooling)
httpx>=0.25.0

# Retry logic
tenacity>=8.0.0

# Structured logging
structlog>=23.0.0

# Legacy HTTP client (for backward compatibility)
requests

# Development and testing
mypy>=1.7.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-httpx>=0.26.0

# Production cache (for Task 25)
redis>=5.0.0
